import { defineStore } from 'pinia'

// initial state
const initialState = () => ({})

export const useHomePageStore = defineStore('HomePageData', {
  state: initialState,
  getters: {},
  actions: {}
})
