# Core content processing - stable versions
markdown==3.9
python-frontmatter==1.1.0
python-dateutil==2.9.0.post0
PyYAML==6.0.2
zipp>=3.19.1 # not directly required, pinned by Snyk to avoid a vulnerability

# ML/AI stack - Python 3.13 compatible versions
numpy>=1.24.0
scipy>=1.10.0
scikit-learn>=1.3.0
nltk>=3.8

# Transformer stack - essential for unified topic extraction
torch>=2.0.0,<2.9.0
transformers>=4.20.0
sentence-transformers>=2.2.0

# Development tools
pur>=7.3.0  # Python dependency updater (equivalent to ncu for npm)

# Advanced topic modeling - made optional due to Python 3.13 constraints
# These will be installed individually with fallback handling in setup script
# bertopic>=0.15.0  
# umap-learn>=0.5.0
# hdbscan>=0.8.0
